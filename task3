using System;

namespace fibonachiiter
{
    class Program
    {
        private static void Main(string[] args)
        {
            Console.WriteLine("До какого числа считать ряд Фибоначчи?");
            int number = int.Parse (Console.ReadLine());
            int first = 0;
            Console.Write(first + " ");
            int next = 1;
            Console.Write(next);
            int sum = 0;
            while (number >= sum)
            {
                sum = first + next;

                Console.Write(" " + sum);

                first = next;
                next = sum;
            }
        }
    }
}